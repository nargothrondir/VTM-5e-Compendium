# То, что нужно в CI. Книг там нет и быть не может — они под .gitignore, —
# поэтому PyMuPDF сюда не входит: импорт его отложен до main() в тех
# скриптах, которым он и правда нужен.
#
# PyYAML нужен сборке глоссария: она читает data/mapping.yaml.
pyyaml>=6.0
