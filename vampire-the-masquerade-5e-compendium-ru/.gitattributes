# Раньше здесь стояло `packs/** binary` — тогда рядом с исходниками лежала
# скомпилированная база LevelDB. Теперь база в .gitignore, а JSON-исходники
# должны быть текстом: их диффы читаются при ревью перевода.
packs/assets/** binary
